/** Shared between the level and the article page: how a trip between them works. */

/** How long the beam-out / beam-in plays before the page changes. Matches
 *  the keyframes in globals.css. */
export const TELEPORT_MS = 800;

/** sessionStorage key: which post the hero left from, so he comes back to it. */
export const RETURN_KEY = "mr-planet:return-to";
